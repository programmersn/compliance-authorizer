# Normalize to LF in the repo and on checkout — keeps file bytes identical
# across Windows/Linux/macOS. (Evidence hashing is over RFC 8785 canonical
# JSON, not raw file bytes, so this is hygiene, not a correctness dependency.)
* text=auto eol=lf
