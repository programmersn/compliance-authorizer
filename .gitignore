# Private local context (machine-local, never commit)
CLAUDE.local.md

# Dependencies / build
node_modules/
dist/
build/
coverage/

# Evidence store (ET14): file-backed SQLite DB written at runtime by `npm run
# dev`/`start`. Synthetic decisions only, but never commit a runtime DB.
data/

# Local env / secrets
.env
.env.local
*.key
*.pem
.keys/

# Editor / OS
.DS_Store
Thumbs.db
*.swp
*~
.idea/
.vscode/

# gstack local state
.gstack/

# Logs
*.log
